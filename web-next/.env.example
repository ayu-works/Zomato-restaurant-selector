# Set on Vercel → Project Settings → Environment Variables.
# Point this at the deployed FastAPI backend (e.g. Render service URL).
# No trailing slash. Used by next.config.mjs to rewrite /api/* server-side.
BACKEND_URL=https://your-fastapi-service.onrender.com
