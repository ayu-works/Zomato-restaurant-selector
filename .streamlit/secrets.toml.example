# Copy to `.streamlit/secrets.toml` for local Streamlit dev; never commit the
# real file. On Streamlit Community Cloud, paste the same key/value pair into
# Settings → Secrets in the dashboard (no file is uploaded).
GROQ_API_KEY = "gsk_xxx_replace_me"
